/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["api-inkubator.solotechnopark.id", "localhost"],
  },
};

module.exports = nextConfig;
